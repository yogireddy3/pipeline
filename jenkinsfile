pipeline {
   agent any 

   stages {
       stage ('build'){
           steps {
               echo 'build the appliation'
           }
       }
       stage ('test'){
           steps {
               echo 'test the appliation'
           }
       }
       stage ('deploy'){
           steps {
               echo 'deploy the appliation'
           }
       }
   }
}
